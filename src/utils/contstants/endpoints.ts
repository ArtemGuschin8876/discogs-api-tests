export enum Endpoints {
    RELEASES = 'releases/',
    STATS = 'stats',
    USERS = 'users/',
    WANTS = 'wants',
    ARTISTS = 'artists/',
    LOGIN = 'login',
    LABELS = 'labels/',
    RELEASE_RATING ='rating',
}